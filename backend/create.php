<?php

require_once "./connection.php";

/* $sql = "CREATE TABLE artikl (
    id INT(6) UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    naziv VARCHAR(30) NOT NULL,
    proizvodac VARCHAR(30) NOT NULL,
    model VARCHAR(30) NOT NULL,
    cijena int(11),
    kolicina int(11)
    )";

if($oConnection->query($sql) == true)
{
    echo "Tablica kreirana";
}
else
{
    echo "Pogreska ".$oConnection->error;
} */

/* $sql = "INSERT INTO artikl (id, naziv, proizvodac, model, cijena, kolicina) VALUES(3, 'Matična ploča', 'Gigabyte', 'AM4X', 980, 0)";

if($oConnection->query($sql) == true)
{
    echo "Proizvod dodan";
}
else
{
    echo "Pogreska: ".$oConnection->error;
} */

